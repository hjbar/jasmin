u8 a = 123;
u8 b = -17;
u8 c = 0xab;
u8 d = 0Xcd;
u8 e = -0XAA;

export fn main_test(reg u32 x) -> reg u32 {
  reg u32 tmp;
  reg u32 x;

  tmp = (32u)a;
  x += tmp;

  tmp = (32u)b;
  x += tmp;

  tmp = (32u)c;
  x += tmp;

  tmp = (32u)d;
  x += tmp;

  tmp = (32u)e;
  x += tmp;

  return x;
}
