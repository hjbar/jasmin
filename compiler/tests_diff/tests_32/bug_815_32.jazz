export
fn snd(reg u32 x y) -> reg u32 {
  stack u32[1] a b;
  a[0] = x;
  b[0] = y;
  a, b = #swap(a, b);
  x = a[0];
  return x;
}

export
fn main_test(reg u32 a) -> reg u32 {
  reg u32 x;
  reg u32 y;
  x = 4 * a;
  y = 2 * a;
  a = snd(x, y);
  return a;
}
