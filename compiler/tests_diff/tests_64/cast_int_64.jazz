inline
fn test() -> inline int
{
    inline int i j;
    i = 0;
    j = 1 << i;
    return j;
}

export
fn main_aux() -> reg u64 {
  reg u64 n;
  inline int k;
  k = test();
  n = k;
  return n;
}

export
fn main_test(reg u64 x) -> reg u64 {
  reg u64 y;
  y = main_aux();
  x -= y;
  return x;
}
