export
fn snd(reg u64 x y) -> reg u64 {
  stack u64[1] a b;
  a[0] = x;
  b[0] = y;
  a, b = #swap(a, b);
  x = a[0];
  return x;
}

export
fn main_test(reg u64 a) -> reg u64 {
  reg u64 x;
  reg u64 y;
  x = 4 * a;
  y = 2 * a;
  a = snd(x, y);
  return a;
}
