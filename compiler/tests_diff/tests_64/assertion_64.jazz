export fn noop() {
  assert("trivial", true);
}

export fn checked(reg u64 a) -> reg u64 {
  if a < 100 {
    assert("basic math", a < 100);
  }
  return a;
}

export fn main_test(reg u64 a) -> reg u64 {
  noop();
  a = checked(a);
  return a;
}
