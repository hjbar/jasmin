param int n = 1;

inline
fn f(reg u64[n] x) -> reg u64[n] { return x; }
export
fn g() -> reg u64 {
reg u64[n] a;
reg u64[n] b;
b[0] = 0;
a = #copy_64(b);
b = f(a);
reg u64 r = b[0];
return r;
}

export
fn main_test(reg u64 x) -> reg u64 {
  reg u64 y;
  y = g();
  x += y;
  return x;
}
